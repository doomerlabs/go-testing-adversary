name: go/testing
version: 0.0.22
description: "Reviews Go tests for broken harnesses, flaky shared state, incomplete cross-entrypoint contracts, and oracles that preserve trivial defects."

triggers:
  manual: true
  files_changed:
    - "**/*_test.go"

detection:
  files:
    - "**/*_test.go"

runtime:
  name: node
  version: "22"
  command:
    - dist/index.js

permissions:
  enforcement: advisory
  filesystem:
    read:
      - .
    write: []
  network: false
  environment:
    allow: []

findings:
  format: adversary.review.v1
